import React from "react";

const ThreadCard = ({
  id,
  currentUserInfo,
  parentId,
  content,
  author,
  community,
  createdAt,
  comments,
}: any) => {
  return (
    <div>
      <h1>ALl threads</h1>
    </div>
  );
};

export default ThreadCard;
